# 2026/06/05

## Phase 2.1 — 19-cell Hex 改為 25-cell 橢圓修正 ROI Grid

---

**Short-term Goal**

- [x] 設計並實作 Phase 2.1：將星座掃描器從 19-cell 六邊形格點改為 25-cell（5×5）橢圓修正矩形格點，並修復 `GetBeamCentersFromSatPos` 未宣告的編譯錯誤

---

**Daily-logs**:

- `09:00 – 10:30`: [Phase 2.1 設計規劃 — 橢圓足跡 + 最大內切矩形 + 25-cell 通道計算](https://github.com/bmw-ece-ntust/TriScale-LEO/tree/12c6e87#phase-21---%E6%A9%A2%E5%9C%93%E4%BF%AE%E6%AD%A3-roi-grid--25-cell-channel-%E8%A8%88%E7%AE%97-phase-21)

- `10:30 – 11:30`: [查閱 0604 日誌 + 確認 orbit-sgp4 現況（GetBeamCentersFromSatPos 未宣告、nBeams=19、靜態 beamCentersEnu 預計算）](https://github.com/bmw-ece-ntust/TriScale-LEO/tree/12c6e87#%E7%8F%BE%E6%B3%81%E5%88%86%E6%9E%900604-%E5%BE%8C)

- `11:30 – 13:00`: [實作 Phase 2.1 — 9 個檔案修改（sat-multi-beam-config/geometry/channel/roi-grid/constellation-scanner）](https://github.com/bmw-ece-ntust/TriScale-LEO/tree/12c6e87#%E6%AA%94%E6%A1%88%E4%BF%AE%E6%94%B9%E6%B8%85%E5%96%AE)

- `14:00 – 14:30`: [編譯錯誤診斷 — sgp4unit.h 找不到（pre-existing path issue，非本次改動造成）](https://github.com/bmw-ece-ntust/TriScale-LEO/tree/12c6e87#%E7%B7%A8%E8%AD%AF%E9%8C%AF%E8%AA%A4%E8%A8%BA%E6%96%B7)

- `<Upcoming>`: 在 VMware ns-3 環境確認 sgp4unit.h 路徑，複製至 scratch/orbit-sgp4/，重新編譯並驗證 25-cell 輸出（cell_idx 0~24，cell 12 beam_gain ≈ 47.7 dBi）

---

## 設計決策

### 問題背景（來自 0604 診斷）

| 問題 | 根本原因 |
|------|---------|
| `GetBeamCentersFromSatPos` 編譯失敗 | `orbit-sgp4/code/sat-multi-beam-geometry.h` 未宣告該函式（phase1/sgp4 已有實作，orbit-sgp4 尚未 port）|
| Beam gain 僅 ~17 dBi（預期 47.7 dBi） | 25-cell 方形格點與 19-beam 六邊形不對齊，cell 落在 sidelobe |
| 仰角修正不正確 | `beamCentersEnu` 以天頂假設靜態預計算，低仰角無橢圓拉伸 |

### Phase 2.1 設計

```
仰角 θ（ROI 中心對衛星）→ 橢圓半軸：
  a (along-track) = r_footprint / sin(θ)
  b (cross-track) = r_footprint

最大內切矩形：
  W_along = a / √2,  H_cross = b / √2

Along-track 方向：satEnu − prevSatEnu（有限差分）
5×5 grid center beam：index 12（row=0, col=0）
cellPos = beamCenters → ΔΦ = 0 → peak gain
```

### nBeams vs nBeamsY 說明

| 參數 | 用途 | 改動 |
|---|---|---|
| `nBeams = 25` | `ComputeFrameResults` 迴圈上限、SINR 干擾加總 | 19 → **25** |
| `nBeamsX = 5` | UPA x 方向 beam port 數，影響天線陣列幾何尺寸（beam gain 計算） | 不變 |
| `nBeamsY` | UPA y 方向 beam port 數，影響天線陣列幾何尺寸 | 4 → **5**（待確認衛星天線配置）|

---

## 修改清單

全部在 `2D/code/orbit-sgp4/code/`（untracked，待 commit）：

| 檔案 | 修改內容 |
|---|---|
| `sat-multi-beam-config.h` | `nBeams 19→25`，`nBeamsY 4→5` |
| `sat-multi-beam-geometry.h` | 新增 `GetBeamCentersFromSatPos`（19-beam，修復編譯錯誤）及 `GetEllipticBeamCenters`（25-beam）宣告 |
| `sat-multi-beam-geometry.cc` | Port `GetBeamCentersFromSatPos` from phase1/sgp4；實作 `GetEllipticBeamCenters` |
| `sat-multi-beam-channel.h` | `array<Vec3,19>→25`，center beam 備註 9→12 |
| `sat-multi-beam-channel.cc` | `array<Vec3,19>→25`，`centreBeamIdx = 9→12` |
| `sat-roi-grid.h` | 新增 `EllipticFootprint` struct + `ComputeEllipticFootprint()` 宣告 |
| `sat-roi-grid.cc` | 實作 `ComputeEllipticFootprint()` |
| `sat-constellation-scanner.h` | `SatScanState`：移除 `beamCentersEnu`，新增 `prevSatEnu`/`hasPrevSat` |
| `sat-constellation-scanner.cc` | `ScanSnrCallback` 改用 `GetEllipticBeamCenters` 25-cell；`Run()` 移除靜態 19-beam 預計算 |

---

## 編譯錯誤診斷

```
fatal error: sgp4unit.h: No such file or directory
```

**原因**：Vallado SGP4 函式庫（`sgp4unit.h/cpp`、`sgp4ext.h/cpp`、`sgp4io.h/cpp`）需從 Hypatia 複製至 scratch/orbit-sgp4/ 目錄。與本次 Phase 2.1 改動無關。

**解法**：
```bash
find /home/wenj/workspace/ns-3.43/contrib -name "sgp4unit.h"
cp /path/to/sgp4unit.h  scratch/orbit-sgp4/
cp /path/to/sgp4unit.cpp scratch/orbit-sgp4/
# 同理 sgp4ext.h/.cpp, sgp4io.h/.cpp
```

---

## 驗證指令

```bash
# 1. 編譯
./ns3 build sat-multi-beam-simulation 2>&1 | grep -E "error:|warning:"

# 2. 執行星座掃描
./ns3 run "sat-multi-beam-simulation \
  --constellation-dir=contrib/satellite/data/scenarios/constellation-iridium-next-66-sats \
  --lat=35.676 --lon=139.65 \
  --window-s=3600 --dt-snr-s=1 \
  --min-elevation-deg=5 \
  --out-dir=scratch/grid25_out" 2>&1 | tee grid25.log

# 預期：
# [Constellation] Phase 2.1 — 5x5 elliptic grid  n_cells=25
# cell_idx 0~24（共 25 格）
# cell 12（中心格）beam_gain_dB ≈ 47.7 dBi（天頂衛星）
```
