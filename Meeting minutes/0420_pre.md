# 0420 會議準備（Layer1 / E2E）

日期：2026-04-20（週一）

## 1) 本次結論（可先講）
- Layer1 的 E2E 改動邏輯完整，路由/觀測範圍/CLI 相容性方向正確。
- `gw2gw_e2e` 的 feeder 顯示 `N/A` 屬設計性診斷結果（非回歸），並有 `FEEDER-DIAG` 訊息可追溯。
- `--mode` / `--pathType` 雙參數衝突檢查、`satStats` 輔助驗證、slot 級 scope 更新都已補上。

## 2) 我們這次要對齊的重點
- `ConnectLinkObserverTraces(useOrbiterFeeder, useGwFeeder)` 改為雙來源觀測，覆蓋 sat2gw 與 gw2gw_e2e 場景差異。
- `UpdateObsScopeForSlot()` 在每個 slot 邊界更新 scope，降低舊 key 造成的誤告警。
- `PrintObsFinalSummary()` 對 gw2gw_e2e 加入 N/A 與診斷輸出，避免把架構繞路誤判成 0 流量故障。
- GW preset 已改成 5 個實體 GW（Tokyo/NewDelhi/Shanghai/SaoPaulo/Mumbai），與 scenario 檔案對齊。

## 3) 明天建議會議流程（15 分鐘）
1. 2 min：先報結論（Layer1 close，功能可進 Layer2）。
2. 5 min：講這次 e2e 觀測鏈修正（feeder / ISL / service 三段）。
3. 4 min：展示兩個關鍵證據
   - gw2gw_e2e：`feeder:* = N/A` + `FEEDER-DIAG`
   - sat2gw：GW 側 feeder 有實際 rx 計數
4. 2 min：風險與邊界（見下節）。
5. 2 min：請主管確認 Layer2 啟動決策。

## 4) 風險與邊界（要主動講）
- 這次是程式邏輯審查 + 既有輸出交叉比對；目前 workspace 沒有可直接執行的 ns-3 建置入口，無法在此機做即時重跑編譯驗證。
- sat2gw 在 slot 切換附近仍可能出現短窗 throughput 告警（已知邊界行為），不影響封包交付結論。

## 5) 建議會中請求（Decision Needed）
- 同意 Layer1 正式結案，Layer2 進入實作。
- 同意把 `satStats=1` 納入 gw2gw_e2e 的回歸檢查模板（作為 feeder_dn 輔助證據）。
- 同意後續補一版「slot 邊界告警抑制」優化（非 blocking）。

## 6) 你可直接使用的口頭版（30 秒）
- 「截至 2026-04-19，Layer1 的 E2E 重構已完成，六種 pathType 的路由與觀測範圍都已對齊。gw2gw 的 feeder 顯示 N/A 是因為 GW2GW_DIRECT 在 REGENERATION_NETWORK 會繞過 feeder PHY，我們已加上診斷訊息避免誤判。整體可正式進入 Layer2，僅保留一項非阻塞優化：slot 邊界的短窗告警抑制。」
